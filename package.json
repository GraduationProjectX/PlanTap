{
  "name": "plantap",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "predev": "node scripts/sync-convex-env.mjs",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "sync:convex-env": "node scripts/sync-convex-env.mjs",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "android": "pnpm --filter mobile android",
    "ios": "pnpm --filter mobile ios"
  },
  "dependencies": {
    "expo": "~55.0.2",
    "react": "19.2.0",
    "react-native": "0.83.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.28.0",
    "oxlint": "^1.42.0",
    "turbo": "^2.8.1",
    "typescript": "~5.9.2"
  },
  "packageManager": "pnpm@10.28.2",
  "pnpm": {
    "overrides": {
      "react": "19.2.0",
      "react-dom": "19.2.0",
      "react-test-renderer": "19.2.0"
    },
    "onlyBuiltDependencies": [
      "@clerk/shared",
      "browser-tabs-lock",
      "bufferutil",
      "core-js",
      "dtrace-provider",
      "esbuild",
      "utf-8-validate"
    ]
  }
}
